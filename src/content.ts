// Content script that runs on web pages
console.log("WordFlow Extension: Content script loaded");

// Note: Text selection is now handled via context menu in background script
// This provides a cleaner, more native browser experience
